# source openstack-venv/bin/activate
# Make sure you're in the openstack-venv (Above command)
# export OS_CLOUD=openstack
# ansible-playbook manage-flavors.yml (To create flavors)
# Verify flavor is created.
# ansible localhost -m os_flavor_facts -a 'name=m2.medium'
# openstack flavor list
# Add below line to the .vimrc in home directory:
# autocmd FileType yaml setlocal ai ts=2 sw=2 et cuc
# 
# Command to pull server list for dynamic inventory in Tower.
# ansible all -i openstack_inventory.py --list-hosts -l "group_apps"
# ansible all -i openstack_inventory.py -m reboot -b
# ansible all -i openstack_inventory.py -m yum -a 'name=* state=latest' -b
